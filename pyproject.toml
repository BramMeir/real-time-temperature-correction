[tool.poetry]
name = "masterproef"
version = "0.1.0"
description = ""
authors = ["Bram Meir <Bram.Meir@ugent.be>"]
readme = "README.md"
package-mode = false

[tool.poetry.dependencies]
python = "^3.12"
pandas = "^2.3.3"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
